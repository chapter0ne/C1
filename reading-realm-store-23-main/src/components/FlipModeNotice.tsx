import React from "react";

const FlipModeNotice = () => (
  <div className="p-4 text-center text-sm text-gray-500">
    <p>
      The Flip reading mode uses a page curl animation for a more tactile experience. You can drag, swipe, or click the page corners to turn pages. If you experience performance issues, switch back to Scroll mode in the settings.
    </p>
  </div>
);

export default FlipModeNotice;
